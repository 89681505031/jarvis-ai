# План: Регистрация пользователя + API ключи + Cleanup

## Обзор задач
1. Создать красивое окно регистрации при первом запуске
2. Удалить жёстко прописанные API ключи из кода
3. Добавить управление API ключами в настройки
4. Удалить папку mobile из git-репозитория
5. Использовать SQLite для хранения данных пользователя
6. Добавить ПОЛНОЦЕННОЕ авто-обновление с прогресс-баром и автоперезапуском
7. Добавить **систему логина/пароля** с синхронизацией через Firebase
8. Добавить **полную синхронизацию** между ПК (память, настройки, история)
9. Добавить **автоматический выход** с другого ПК

## Файлы для модификации

### 1. `jarvis_fixed.py` — основной файл
- Удалить `GIGACHAT_BUILTIN_KEY` и `FISH_AUDIO_BUILTIN_KEY`
- Убрать автоматическую подстановку встроенных ключей
- Добавить окно регистрации (первый запуск)
- Добавить секцию API ключей в настройки
- Добавить кнопку "Профиль" в интерфейс
- Удалить `self.user_name = ""` и заменить на SQLite

### 2. `config.json` — конфигурация
- Удалить `gigachat_auth_key` (перенести в SQLite)

### 3. `.gitignore` — игнорирование
- Добавить `mobile/`
- Добавить `*.db`, `*.sqlite`

### 4. Git — cleanup
- `git rm -r --cached mobile/`
- Удалить из истории (если нужно)

---

## Детальный план

### Шаг 1: Создание SQLite модуля для хранения данных

**Новый файл:** `user_database.py`

```python
import sqlite3
import os
from pathlib import Path

class UserDatabase:
    def __init__(self, db_path=None):
        if db_path is None:
            db_path = Path(__file__).parent / "jarvis_users.db"
        self.db_path = db_path
        self._init_db()
    
    def _init_db(self):
        conn = sqlite3.connect(self.db_path)
        cursor = conn.cursor()
        
        # Таблица пользователей
        cursor.execute('''
            CREATE TABLE IF NOT EXISTS users (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                name TEXT NOT NULL,
                birth_date TEXT,
                birth_month TEXT,
                birth_year INTEGER,
                created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
            )
        ''')
        
        # Таблица API ключей
        cursor.execute('''
            CREATE TABLE IF NOT EXISTS api_keys (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                service TEXT NOT NULL UNIQUE,
                api_key TEXT NOT NULL,
                created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
            )
        ''')
        
        conn.commit()
        conn.close()
    
    def set_user(self, name, birth_date, birth_month, birth_year):
        conn = sqlite3.connect(self.db_path)
        cursor = conn.cursor()
        cursor.execute('''
            INSERT OR REPLACE INTO users (id, name, birth_date, birth_month, birth_year)
            VALUES (1, ?, ?, ?, ?)
        ''', (name, birth_date, birth_month, birth_year))
        conn.commit()
        conn.close()
    
    def get_user(self):
        conn = sqlite3.connect(self.db_path)
        conn.row_factory = sqlite3.Row
        cursor = conn.cursor()
        cursor.execute('SELECT * FROM users WHERE id = 1')
        user = cursor.fetchone()
        conn.close()
        return dict(user) if user else None
    
    def set_api_key(self, service, api_key):
        conn = sqlite3.connect(self.db_path)
        cursor = conn.cursor()
        cursor.execute('''
            INSERT OR REPLACE INTO api_keys (service, api_key)
            VALUES (?, ?)
        ''', (service, api_key))
        conn.commit()
        conn.close()
    
    def get_api_key(self, service):
        conn = sqlite3.connect(self.db_path)
        conn.row_factory = sqlite3.Row
        cursor = conn.cursor()
        cursor.execute('SELECT api_key FROM api_keys WHERE service = ?', (service,))
        row = cursor.fetchone()
        conn.close()
        return row['api_key'] if row else None
    
    def has_api_key(self, service):
        return self.get_api_key(service) is not None
```

### Шаг 2: Удаление встроенных ключей из `jarvis_fixed.py`

**Удалить:**
- `FISH_AUDIO_BUILTIN_KEY = "sk-fish-TSwmQZcWu4kesmD6NjBdmHBbrWFfhFyK2hXkDy_EZVA"`
- `GIGACHAT_BUILTIN_KEY = 'MDFhMDU5YjItYjhjNy03NGJjLWI4YWUtNDg5YTVjZTQ2Mzg5OmVkMzRiNzhlLTIzNzMtNGI2NC05M2ZlLWVkZjkwNmJlNmQ2MA=='`

**Заменить блок инициализации GigaChat:**
```python
# Старый код:
GIGACHAT_AUTH_KEY = os.environ.get('GIGACHAT_AUTH_KEY', '')
GIGACHAT_BUILTIN_KEY = '...'
if not GIGACHAT_AUTH_KEY:
    # Пробуем config.json
    ...
    if not GIGACHAT_AUTH_KEY:
        GIGACHAT_AUTH_KEY = GIGACHAT_BUILTIN_KEY

# Новый код:
from user_database import UserDatabase
db = UserDatabase()
GIGACHAT_AUTH_KEY = db.get_api_key('gigachat') or os.environ.get('GIGACHAT_AUTH_KEY', '')
FISH_AUDIO_API_KEY = db.get_api_key('fish_audio') or os.environ.get('FISH_AUDIO_API_KEY', '')
```

### Шаг 3: Создание окна регистрации

**Метод:** `show_registration_window()`

```python
def show_registration_window(self):
    """Показывает красивое окно регистрации при первом запуске"""
    reg_win = tk.Toplevel(self)
    reg_win.title("Добро пожаловать в J.A.R.V.I.S.")
    reg_win.geometry("550x500")
    reg_win.configure(bg="#1e293b")
    reg_win.resizable(False, False)
    reg_win.grab_set()
    
    # Центрирование
    x = self.winfo_x() + (self.winfo_width() - 550) // 2
    y = self.winfo_y() + (self.winfo_height() - 500) // 2
    reg_win.geometry(f"+{x}+{y}")
    
    # Заголовок
    tk.Label(
        reg_win, text="🤖 J.A.R.V.I.S.",
        font=("Segoe UI", 20, "bold"),
        bg="#1e293b", fg="#38bdf8"
    ).pack(pady=(30, 10))
    
    tk.Label(
        reg_win, text="Персональный ИИ-ассистент",
        font=("Segoe UI", 11),
        bg="#1e293b", fg="#94a3b8"
    ).pack(pady=(0, 25))
    
    # Разделитель
    tk.Frame(reg_win, bg="#334155", height=2).pack(fill=tk.X, padx=40, pady=10)
    
    # Секция 1: Личные данные
    tk.Label(
        reg_win, text="👤 ЛИЧНЫЕ ДАННЫЕ",
        font=("Segoe UI", 11, "bold"),
        bg="#1e293b", fg="#38bdf8"
    ).pack(pady=(15, 8))
    
    # Имя
    tk.Label(reg_win, text="Ваше имя:", font=("Segoe UI", 9), bg="#1e293b", fg="#cbd5e1").pack(anchor="w", padx=40, pady=(5, 2))
    name_entry = tk.Entry(reg_win, font=("Segoe UI", 11), bg="#0f172a", fg="#e2e8f0", relief=tk.SOLID, bd=1, width=40)
    name_entry.pack(pady=(0, 8), padx=40)
    
    # Дата рождения
    tk.Label(reg_win, text="Дата рождения:", font=("Segoe UI", 9), bg="#1e293b", fg="#cbd5e1").pack(anchor="w", padx=40, pady=(5, 2))
    
    date_frame = tk.Frame(reg_win, bg="#1e293b")
    date_frame.pack(pady=(0, 8), padx=40)
    
    tk.Entry(date_frame, font=("Segoe UI", 10), bg="#0f172a", fg="#e2e8f0", relief=tk.SOLID, bd=1, width=8).pack(side=tk.LEFT, padx=2)
    tk.Label(date_frame, text="-", font=("Segoe UI", 10), bg="#1e293b", fg="#64748b").pack(side=tk.LEFT, padx=5)
    tk.Entry(date_frame, font=("Segoe UI", 10), bg="#0f172a", fg="#e2e8f0", relief=tk.SOLID, bd=1, width=12).pack(side=tk.LEFT, padx=2)
    tk.Label(date_frame, text="-", font=("Segoe UI", 10), bg="#1e293b", fg="#64748b").pack(side=tk.LEFT, padx=5)
    tk.Entry(date_frame, font=("Segoe UI", 10), bg="#0f172a", fg="#e2e8f0", relief=tk.SOLID, bd=1, width=8).pack(side=tk.LEFT, padx=2)
    
    # Разделитель
    tk.Frame(reg_win, bg="#334155", height=2).pack(fill=tk.X, padx=40, pady=15)
    
    # Секция 2: API ключи
    tk.Label(
        reg_win, text="🔑 API КЛЮЧИ (опционально)",
        font=("Segoe UI", 11, "bold"),
        bg="#1e293b", fg="#38bdf8"
    ).pack(pady=(0, 8))
    
    # GigaChat
    tk.Label(reg_win, text="GigaChat API:", font=("Segoe UI", 9), bg="#1e293b", fg="#cbd5e1").pack(anchor="w", padx=40, pady=(5, 2))
    gigachat_entry = tk.Entry(reg_win, font=("Segoe UI", 10), bg="#0f172a", fg="#e2e8f0", relief=tk.SOLID, bd=1, width=40, show="*")
    gigachat_entry.pack(pady=(0, 8), padx=40)
    
    # Fish Audio
    tk.Label(reg_win, text="Fish Audio API:", font=("Segoe UI", 9), bg="#1e293b", fg="#cbd5e1").pack(anchor="w", padx=40, pady=(5, 2))
    fish_entry = tk.Entry(reg_win, font=("Segoe UI", 10), bg="#0f172a", fg="#e2e8f0", relief=tk.SOLID, bd=1, width=40, show="*")
    fish_entry.pack(pady=(0, 8), padx=40)
    
    # Кнопка "Начать"
    def save_registration():
        name = name_entry.get().strip()
        if not name:
            messagebox.showwarning("Внимание", "Пожалуйста, введите ваше имя")
            return
        
        # Сохраняем в БД
        db.set_user(name, "01", "января", 2000)  # Упрощённо
        
        # Сохраняем API ключи
        gigachat_key = gigachat_entry.get().strip()
        if gigachat_key:
            db.set_api_key('gigachat', gigachat_key)
        
        fish_key = fish_entry.get().strip()
        if fish_key:
            db.set_api_key('fish_audio', fish_key)
        
        reg_win.destroy()
        self.after(100, self.show_welcome)
    
    ModernButton(
        reg_win, text="🚀 НАЧАТЬ РАБОТУ",
        font=("Segoe UI", 12, "bold"),
        bg="#2563eb", fg="#fff",
        command=save_registration,
        padx=30, pady=12
    ).pack(pady=(15, 10))
```

### Шаг 4: Добавить кнопку "Профиль" в интерфейс

**В `init_ui()`:**
- Добавить кнопку "👤 ПРОФИЛЬ" рядом с кнопками настроек
- Метод `open_profile()` для просмотра/редактирования данных

```python
def open_profile(self):
    """Открывает окно профиля пользователя"""
    profile_win = tk.Toplevel(self)
    profile_win.title("👤 Профиль")
    profile_win.geometry("500x400")
    profile_win.configure(bg=self.panel_bg)
    profile_win.grab_set()
    
    user = db.get_user()
    if user:
        tk.Label(profile_win, text=f"Имя: {user['name']}", ...).pack()
        # ... и т.д.
```

### Шаг 5: Добавить секцию API ключей в настройки

**В `open_settings()`:**
- После секции персонажей добавить "🔑 API КЛЮЧИ"
- Поля для GigaChat и Fish Audio
- Кнопки сохранения

```python
def save_gigachat_key():
    new_key = self.gigachat_key_var.get().strip()
    if new_key:
        db.set_api_key('gigachat', new_key)
        messagebox.showinfo("Успех", "API ключ GigaChat сохранён!")

def save_fish_key():
    new_key = self.fish_api_key_var.get().strip()
    if new_key:
        db.set_api_key('fish_audio', new_key)
        messagebox.showinfo("Успех", "API ключ Fish Audio сохранён!")
```

### Шаг 6: Firebase Realtime Database — синхронизация между ПК

**Новый файл:** `firebase_sync.py`

```python
import json
import os
import threading
import time
import hashlib
from pathlib import Path

try:
    import firebase_admin
    from firebase_admin import credentials, db, auth
    FIREBASE_OK = True
except ImportError:
    FIREBASE_OK = False
    print("Firebase не установлен: pip install firebase-admin")

class FirebaseSync:
    """Синхронизация данных между ПК через Firebase"""
    
    def __init__(self, app):
        self.app = app
        self.user_id = None
        self.current_session = None
        self.is_connected = False
        self._sync_lock = threading.Lock()
        self._last_sync_data = {}
        
        if FIREBASE_OK:
            self._init_firebase()
    
    def _init_firebase(self):
        """Инициализация Firebase"""
        try:
            # Проверяем, не инициализирован ли уже
            if firebase_admin._apps:
                self.db = firebase_admin.db
                return
            
            # Загрузка credentials
            cred_path = Path(__file__).parent / "firebase_credentials.json"
            if cred_path.exists():
                cred = credentials.Certificate(str(cred_path))
                firebase_admin.initialize_app(cred, {
                    'databaseURL': 'https://jarvis-ai-default-rtdb.firebaseio.com'
                })
            else:
                print("Firebase credentials не найдены")
                return
            
            self.db = firebase_admin.db
        except Exception as e:
            print(f"Firebase init error: {e}")
    
    def _hash_password(self, password):
        """Хеширование пароля"""
        return hashlib.sha256(password.encode()).hexdigest()
    
    def register_user(self, email, password, user_data):
        """
        Регистрация нового пользователя
        
        Args:
            email: Email пользователя
            password: Пароль
            user_data: Словарь с данными (имя, дата рождения и т.д.)
        
        Returns:
            bool: True если успешно
        """
        if not FIREBASE_OK:
            print("Firebase не доступен")
            return False
        
        try:
            # Создаём пользователя в Firebase Auth
            user = auth.create_user(
                email=email,
                password=password
            )
            
            # Сохраняем данные пользователя в Realtime Database
            user_ref = self.db.reference(f'users/{user.uid}')
            user_ref.set({
                'email': email,
                'data': user_data,
                'created_at': time.time(),
                'last_login': time.time(),
                'active_sessions': {}
            })
            
            print(f"✅ Пользователь зарегистрирован: {user.uid}")
            return True
            
        except Exception as e:
            print(f"❌ Регистрация ошибка: {e}")
            return False
    
    def login_user(self, email, password):
        """
        Вход пользователя
        
        Args:
            email: Email
            password: Пароль
        
        Returns:
            tuple: (success, user_data, session_id)
        """
        if not FIREBASE_OK:
            return False, None, None
        
        try:
            # Аутентификация через Firebase Auth
            # Используем email/link password auth
            from firebase_admin import auth as firebase_auth
            
            # Получаем пользователя по email
            user = firebase_auth.get_user_by_email(email)
            
            # Проверяем пароль (Firebase Auth делает это internally)
            # Создаём custom token для проверки
            password_hash = self._hash_password(password)
            
            # Получаем данные пользователя
            user_ref = self.db.reference(f'users/{user.uid}')
            user_data = user_ref.get()
            
            if not user_data:
                return False, None, None
            
            # Проверяем хеш пароля
            stored_hash = user_data.get('password_hash')
            if stored_hash != password_hash:
                return False, None, None
            
            # Создаём сессию
            import uuid
            session_id = str(uuid.uuid4())
            pc_info = self._get_pc_info()
            
            # Сохраняем сессию
            user_ref.child('active_sessions').child(session_id).set({
                'pc_info': pc_info,
                'login_time': time.time(),
                'last_active': time.time()
            })
            
            # Обновляем last_login
            user_ref.update({'last_login': time.time()})
            
            self.user_id = user.uid
            self.current_session = session_id
            self.is_connected = True
            
            print(f"✅ Вход выполнен: {session_id}")
            return True, user_data.get('data'), session_id
            
        except Exception as e:
            print(f"❌ Вход ошибка: {e}")
            return False, None, None
    
    def auto_login(self, user_id):
        """
        Автоматический вход с сохранённой сессией
        
        Args:
            user_id: UID пользователя из Firebase
        
        Returns:
            tuple: (success, user_data, session_id)
        """
        if not FIREBASE_OK:
            return False, None, None
        
        try:
            # Получаем данные пользователя
            user_ref = self.db.reference(f'users/{user_id}')
            user_data = user_ref.get()
            
            if not user_data:
                return False, None, None
            
            # Проверяем активные сессии
            sessions = user_ref.child('active_sessions').get()
            
            if not sessions:
                # Нет активных сессий - нужно ввести пароль
                return False, None, None
            
            # Создаём новую сессию
            import uuid
            session_id = str(uuid.uuid4())
            pc_info = self._get_pc_info()
            
            # Сохраняем сессию
            user_ref.child('active_sessions').child(session_id).set({
                'pc_info': pc_info,
                'login_time': time.time(),
                'last_active': time.time()
            })
            
            self.user_id = user_id
            self.current_session = session_id
            self.is_connected = True
            
            print(f"✅ Авто-вход выполнен: {session_id}")
            return True, user_data.get('data'), session_id
            
        except Exception as e:
            print(f"❌ Авто-вход ошибка: {e}")
            return False, None, None
    
    def _get_pc_info(self):
        """Получает уникальную информацию ПК"""
        import platform
        import uuid
        
        # Генерируем уникальный ID для ПК
        pc_id_path = Path(__file__).parent / ".pc_id"
        if pc_id_path.exists():
            pc_id = pc_id_path.read_text().strip()
        else:
            pc_id = str(uuid.uuid4())
            pc_id_path.write_text(pc_id)
        
        return {
            'pc_id': pc_id,
            'os': platform.system(),
            'hostname': platform.node()
        }
    
    def sync_data(self, data):
        """
        Синхронизация данных с Firebase
        
        Args:
            data: Словарь с данными для синхронизации
        """
        if not self.is_connected or not FIREBASE_OK:
            return
        
        with self._sync_lock:
            try:
                sync_ref = self.db.reference(f'sync/{self.user_id}')
                sync_ref.update({
                    'data': data,
                    'last_sync': time.time(),
                    'session': self.current_session
                })
                self._last_sync_data = data
                print("✅ Данные синхронизированы")
            except Exception as e:
                print(f"❌ Синхронизация ошибка: {e}")
    
    def load_synced_data(self):
        """
        Загрузка синхронизированных данных
        
        Returns:
            dict: Синхронизированные данные
        """
        if not self.is_connected or not FIREBASE_OK:
            return None
        
        try:
            sync_ref = self.db.reference(f'sync/{self.user_id}')
            data = sync_ref.get()
            if data and 'data' in data:
                print("✅ Данные загружены из Firebase")
                return data['data']
            return None
        except Exception as e:
            print(f"❌ Загрузка данных ошибка: {e}")
            return None
    
    def listen_for_changes(self, callback):
        """
        Слушает изменения в Firebase
        
        Args:
            callback: Функция, вызываемая при изменении данных
        """
        if not self.is_connected or not FIREBASE_OK:
            return
        
        def on_change(event):
            if event.event_type in ['put', 'patch']:
                data = event.snapshot.value
                if data and 'data' in data:
                    # Проверяем, что это не наша сессия
                    session = data.get('session')
                    if session != self.current_session:
                        callback(data['data'])
        
        try:
            sync_ref = self.db.reference(f'sync/{self.user_id}')
            sync_ref.listen(on_change)
            print("👂 Слушаем изменения...")
        except Exception as e:
            print(f"❌ Слушатель ошибка: {e}")
    
    def logout_current_session(self):
        """Выход из текущей сессии"""
        if not self.is_connected or not FIREBASE_OK:
            return
        
        try:
            user_ref = self.db.reference(f'users/{self.user_id}')
            user_ref.child('active_sessions').child(self.current_session).delete()
            self.is_connected = False
            self.current_session = None
            print("🚪 Выход выполнен")
        except Exception as e:
            print(f"❌ Выход ошибка: {e}")
    
    def check_other_sessions(self):
        """
        Проверяет, есть ли другие активные сессии
        
        Returns:
            bool: True если есть другие сессии
        """
        if not self.is_connected or not FIREBASE_OK:
            return False
        
        try:
            user_ref = self.db.reference(f'users/{self.user_id}')
            sessions = user_ref.child('active_sessions').get()
            
            if sessions:
                other_sessions = [s for s in sessions.keys() if s != self.current_session]
                if other_sessions:
                    print(f"⚠️ Найдены другие сессии: {len(other_sessions)}")
                    return True
            return False
        except Exception as e:
            print(f"❌ Проверка сессий ошибка: {e}")
            return False
    
    def force_logout_all(self):
        """Принудительный выход со всех устройств"""
        if not self.is_connected or not FIREBASE_OK:
            return
        
        try:
            user_ref = self.db.reference(f'users/{self.user_id}')
            user_ref.child('active_sessions').delete()
            self.logout_current_session()
            print("🚪 Выход со всех устройств")
        except Exception as e:
            print(f"❌ Выход ошиб��а: {e}")
```

**Конфигурация Firebase:**

1. Создаём проект на https://console.firebase.google.com/
2. Название: `jarvis-ai`
3. Включаем Realtime Database
4. Скачиваем `firebase_credentials.json`
5. Кладем в корень проекта
6. Добавляем в `.gitignore`

**Firebase Database Rules:**
```json
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "sync": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
```

```bash
git rm -r --cached mobile/
echo "mobile/" >> .gitignore
git commit -m "chore: remove mobile folder from repository"
git push
```

### Шаг 7: Система логина/пароля с автосохранением сессии

**Логика работы:**
- При первом запуске → регистрация (email + пароль + данные)
- При следующем запуске → **автоматический вход** (если не вышли)
- Кнопка "Выйти" в профиле → выход со всех устройств
- Галочка "Запомнить меня" → сессия сохраняется навсегда
- При входе с другого ПК → первый ПК получает сигнал "выйти"

**Методы в `jarvis_fixed.py`:**

```python
def show_login_window(self):
    """Показывает окно входа/регистрации"""
    login_win = tk.Toplevel(self)
    login_win.title("🔐 J.A.R.V.I.S. - Вход")
    login_win.geometry("500x450")
    login_win.configure(bg="#1e293b")
    login_win.resizable(False, False)
    login_win.grab_set()
    
    # Центрирование
    x = self.winfo_x() + (self.winfo_width() - 500) // 2
    y = self.winfo_y() + (self.winfo_height() - 450) // 2
    login_win.geometry(f"+{x}+{y}")
    
    # Заголовок
    tk.Label(
        login_win, text="🔐 J.A.R.V.I.S.",
        font=("Segoe UI", 20, "bold"),
        bg="#1e293b", fg="#38bdf8"
    ).pack(pady=(30, 10))
    
    tk.Label(
        login_win, text="Вход в аккаунт",
        font=("Segoe UI", 11),
        bg="#1e293b", fg="#94a3b8"
    ).pack(pady=(0, 25))
    
    # Разделитель
    tk.Frame(login_win, bg="#334155", height=2).pack(fill=tk.X, padx=40, pady=10)
    
    # Email
    tk.Label(login_win, text="Email:", font=("Segoe UI", 9), bg="#1e293b", fg="#cbd5e1").pack(anchor="w", padx=40, pady=(10, 2))
    email_entry = tk.Entry(login_win, font=("Segoe UI", 11), bg="#0f172a", fg="#e2e8f0", relief=tk.SOLID, bd=1, width=40)
    email_entry.pack(pady=(0, 8), padx=40)
    email_entry.insert(0, "user@example.com")  # Placeholder
    
    # Пароль
    tk.Label(login_win, text="Пароль:", font=("Segoe UI", 9), bg="#1e293b", fg="#cbd5e1").pack(anchor="w", padx=40, pady=(10, 2))
    password_entry = tk.Entry(login_win, font=("Segoe UI", 11), bg="#0f172a", fg="#e2e8f0", relief=tk.SOLID, bd=1, width=40, show="*")
    password_entry.pack(pady=(0, 8), padx=40)
    
    # Кнопка показать/скрыть пароль
    def toggle_password():
        if password_entry.cget('show') == '*':
            password_entry.config(show='')
            toggle_btn.config(text="🙈")
        else:
            password_entry.config(show='*')
            toggle_btn.config(text="👁")
    
    toggle_btn = tk.Button(
        login_win, text="👁",
        font=("Segoe UI", 10),
        bg="#334155", fg="#e2e8f0",
        command=toggle_password,
        relief=tk.FLAT,
        width=3
    )
    toggle_btn.pack(pady=(0, 5), padx=40)
    
    # Разделитель
    tk.Frame(login_win, bg="#334155", height=2).pack(fill=tk.X, padx=40, pady=15)
    
    # Кнопки
    def login():
        email = email_entry.get().strip()
        password = password_entry.get().strip()
        
        if not email or not password:
            messagebox.showwarning("Внимание", "Введите email и пароль")
            return
        
        # Пытаемся войти
        success, user_data, session_id = firebase_sync.login_user(email, password)
        
        if success:
            # Сохраняем user_id локально
            db.set_api_key('firebase_user_id', user_data.get('firebase_uid', ''))
            
            # Загружаем синхронизированные данные
            synced_data = firebase_sync.load_synced_data()
            if synced_data:
                # Восстанавливаем память
                self._restore_memory_from_sync(synced_data.get('memory', {}))
                # Восстанавливаем настройки
                self._restore_settings_from_sync(synced_data.get('settings', {}))
            
            login_win.destroy()
            self.after(100, self.show_welcome)
            
            # Начинаем слушать изменения
            firebase_sync.listen_for_changes(self._on_sync_change)
            
            # Проверяем другие сессии
            self.after(2000, self._check_other_sessions)
        else:
            messagebox.showerror("Ошибка", "Неверный email или пароль")
    
    def register():
        """Открывает окно регистрации"""
        login_win.destroy()
        self.show_registration_window()
    
    ModernButton(
        login_win, text="🔓 ВХОД",
        font=("Segoe UI", 12, "bold"),
        bg="#2563eb", fg="#fff",
        command=login,
        padx=30, pady=12
    ).pack(pady=(10, 5))
    
    register_btn = tk.Label(
        login_win, text="Нет аккаунта? Зарегистрироваться",
        font=("Segoe UI", 9),
        bg="#1e293b", fg="#38bdf8",
        cursor="hand2"
    )
    register_btn.pack(pady=(0, 15))
    register_btn.bind("<Button-1>", lambda e: register())
```

**Метод для обработки изменений из Firebase:**

```python
def _on_sync_change(self, new_data):
    """Обработка изменений данных из Firebase"""
    try:
        # Обновляем память
        if 'memory' in new_data:
            self._load_persistent_memory()
            log.info("✅ Память обновлена из Firebase")
        
        # Обновляем настройки
        if 'settings' in new_data:
            self.config_data.update(new_data['settings'])
            self.save_config()
            log.info("✅ Настройки обновлены из Firebase")
        
        # Озвучиваем обновление
        self.speak_jarvis_free("Данные синхронизированы с облаком")
        
    except Exception as e:
        log.error(f"Ошибка синхронизации: {e}")

def _check_other_sessions(self):
    """Проверяет другие активные сессии"""
    if firebase_sync.check_other_sessions():
        # Показываем предупреждение
        self.after(0, lambda: messagebox.showwarning(
            "Внимание",
            "Обнаружен вход с другого устройства.\n"
            "Вы будете автоматически вышли при входе с того устройства."
        ))
```

**Изменения в `__init__()`:**

```python
# После инициализации
self.firebase_sync = FirebaseSync(self)

# Проверяем, есть ли сохранённый user_id
saved_user_id = db.get_api_key('firebase_user_id')
if saved_user_id:
    # Пользователь уже входил - пробуем автоматический вход
    self._auto_login(saved_user_id)
else:
    # Первый запуск - показываем регистрацию
    self.after(500, self.show_registration_window)

def _auto_login(self, user_id):
    """Автоматический вход с сохранённой сессией"""
    try:
        # Пытаемся войти без окна
        success, user_data, session_id = self.firebase_sync.auto_login(user_id)
        
        if success:
            # Успешный вход - запускаем JARVIS
            self.after(100, self.show_welcome)
            
            # Начинаем слушать изменения
            self.firebase_sync.listen_for_changes(self._on_sync_change)
            
            # Проверяем другие сессии
            self.after(2000, self._check_other_sessions)
            log.info("✅ Автоматический вход выполнен")
        else:
            # Сессия истекла - показываем окно входа
            log.info("⚠️ Сессия истекла, показываем окно входа")
            self.after(500, self.show_login_window)
    except Exception as e:
        log.error(f"❌ Авто-вход ошибка: {e}")
        # Показываем окно входа как fallback
        self.after(500, self.show_login_window)
```

**Заменить текущий `check_for_updates()` и `show_update_dialog()` на:**

```python
# === ГЛОБАЛЬНЫЕ ПЕРЕМЕННЫЕ ДЛЯ ОБНОВЛЕНИЯ ===
self.update_progress = None
self.update_window = None
self.update_download_thread = None

def check_for_updates(self):
    """Проверяет обновления через GitHub API"""
    try:
        import requests
        
        api_url = f"https://api.github.com/repos/{GITHUB_REPO}/releases/latest"
        response = requests.get(api_url, timeout=10, verify=False)
        
        if response.status_code == 200:
            release = response.json()
            latest_version = release.get('tag_name', '1.0.0').lstrip('v')
            
            if latest_version > CURRENT_VERSION:
                log.info(f"🔄 Доступна новая версия: {latest_version}")
                self.after(0, lambda: self.show_update_dialog(latest_version, release))
            else:
                log.info(f"✅ Версия {CURRENT_VERSION} - актуальная")
        elif response.status_code == 404:
            log.info(f"✅ Версия {CURRENT_VERSION} - актуальная")
    except Exception as e:
        log.info(f"⚠️ Проверка обновлений: {e}")

def show_update_dialog(self, latest_version, release):
    """Показывает окно обновления с прогресс-баром"""
    # Создаём окно обновления
    self.update_window = tk.Toplevel(self)
    self.update_window.title(f"🔄 Обновление JARVIS v{latest_version}")
    self.update_window.geometry("550x400")
    self.update_window.resizable(False, False)
    self.update_window.configure(bg="#1e293b")
    self.update_window.grab_set()
    
    # Центрирование
    x = self.winfo_x() + (self.winfo_width() - 550) // 2
    y = self.winfo_y() + (self.winfo_height() - 400) // 2
    self.update_window.geometry(f"+{x}+{y}")
    
    # Заголовок
    tk.Label(
        self.update_window,
        text=f"🔄 Доступна новая версия: {latest_version}",
        font=("Segoe UI", 14, "bold"),
        bg="#1e293b", fg="#38bdf8"
    ).pack(pady=(20, 10))
    
    # Описание
    changelog = release.get('body', 'Исправления и улучшения')
    tk.Label(
        self.update_window,
        text=f"Текущая: {CURRENT_VERSION}\nНовая: {latest_version}",
        font=("Segoe UI", 11),
        bg="#1e293b", fg="#e2e8f0"
    ).pack(pady=10)
    
    # Прогресс-бар
    progress_frame = tk.Frame(self.update_window, bg="#1e293b")
    progress_frame.pack(fill=tk.X, padx=40, pady=20)
    
    self.update_progress = tk.Label(
        progress_frame,
        text="0%",
        font=("Segoe UI", 16, "bold"),
        bg="#1e293b", fg="#2563eb"
    )
    self.update_progress.pack()
    
    self.progress_bar = ttk.Progressbar(
        progress_frame,
        mode='determinate',
        maximum=100,
        style='Custom.TProgressbar'
    )
    self.progress_bar.pack(fill=tk.X, pady=10)
    
    # Стиль для прогресс-бара
    style = ttk.Style()
    style.configure('Custom.TProgressbar', troughcolor="#334155", background="#2563eb", thickness=8)
    
    # Статус
    self.update_status = tk.Label(
        self.update_window,
        text="Инициализация...",
        font=("Segoe UI", 9),
        bg="#1e293b", fg="#94a3b8"
    )
    self.update_status.pack(pady=10)
    
    # Кнопка отмены (только до начала загрузки)
    self.cancel_btn = ModernButton(
        self.update_window,
        text="❌ ОТМЕНА",
        font=("Segoe UI", 10),
        bg="#dc2626", fg="#fff",
        command=self.cancel_update,
        padx=20, pady=8
    )
    self.cancel_btn.pack(pady=10)
    
    # Запуск загрузки в фоне
    self._start_update(latest_version, release)

def _start_update(self, latest_version, release):
    """Запускает обновление в фоновом потоке"""
    def download_and_update():
        try:
            # Получаем URL ZIP артефакта
            assets = release.get('assets', [])
            zip_url = None
            for asset in assets:
                if asset['name'].endswith('.zip'):
                    zip_url = asset['browser_download_url']
                    break
            
            if not zip_url:
                self._update_status("❌ Не найден ZIP файл в релизе")
                return
            
            self._update_status("⬇️ Загрузка обновления...")
            self._update_progress(0)
            self.cancel_btn.config(state=tk.DISABLED)  # Отключаем отмену
            
            # Скачиваем ZIP
            import requests
            import tempfile
            
            temp_dir = tempfile.mkdtemp()
            zip_path = os.path.join(temp_dir, f"JARVIS_PRO_v{latest_version}.zip")
            
            response = requests.get(zip_url, stream=True, timeout=30, verify=False)
            total_size = int(response.headers.get('content-length', 0))
            downloaded = 0
            
            with open(zip_path, 'wb') as f:
                for chunk in response.iter_content(chunk_size=8192):
                    if chunk:
                        f.write(chunk)
                        downloaded += len(chunk)
                        if total_size:
                            percent = int((downloaded / total_size) * 100)
                            self._update_progress(percent)
            
            self._update_progress(100)
            self._update_status("✅ Загрузка завершена! Распаковка...")
            
            # Распаковываем
            extract_dir = os.path.join(temp_dir, "extracted")
            os.makedirs(extract_dir)
            
            with zipfile.ZipFile(zip_path, 'r') as zip_ref:
                zip_ref.extractall(extract_dir)
            
            self._update_status("🔄 Перезапись файлов...")
            
            # === СОХРАНЕНИЕ ВСЕХ ПОЛЬЗОВАТЕЛЬСКИХ ДАННЫХ ===
            current_dir = os.path.dirname(os.path.abspath(__file__))
            backup_dir = os.path.join(temp_dir, "user_data_backup")
            os.makedirs(backup_dir)
            
            # Список всех файлов, которые нужно сохранить:
            # 1. SQLite БД
            # 2. memory.json (память Джарвиса)
            # 3. media_state.json (состояние медиа)
            # 4. jarvis_license.json (подписка)
            # 5. generated_keys.json (ключи подписки)
            # 6. config.json (настройки)
            # 7. Папка sounds/ (пользовательские MP3)
            # 8. Папка images/ (пользовательские изображения)
            # 9. jarvis.log (логи - опционально)
            
            files_to_preserve = [
                "user_database.db", "jarvis_users.db",  # SQLite БД
                "memory.json",  # Память Джарвиса
                "media_state.json",  # Состояние медиа
                "jarvis_license.json",  # Подписка
                "generated_keys.json",  # Ключи подписки
                "config.json",  # Настройки
                "jarvis.log",  # Логи
            ]
            
            preserved_files = {}
            
            for filename in files_to_preserve:
                src_file = os.path.join(current_dir, filename)
                if os.path.exists(src_file):
                    dst_file = os.path.join(backup_dir, filename)
                    shutil.copy2(src_file, dst_file)
                    preserved_files[filename] = dst_file
                    log.info(f"✅ Сохранён файл: {filename}")
            
            # Сохранение папок
            dirs_to_preserve = ["sounds", "images"]
            for dirname in dirs_to_preserve:
                src_dir = os.path.join(current_dir, dirname)
                if os.path.exists(src_dir):
                    dst_dir = os.path.join(backup_dir, dirname)
                    shutil.copytree(src_dir, dst_dir)
                    preserved_files[dirname + "/"] = dst_dir
                    log.info(f"✅ Сохранена папка: {dirname}/")
            
            self._update_status("💾 Сохранено файлов: " + str(len(preserved_files)))
            
            # === КОПИРОВАНИЕ ОБНОВЛЕНИЯ ===
            extracted_dir = os.path.join(extract_dir, "JARVIS_PRO")
            
            if os.path.exists(extracted_dir):
                # Удаляем старые файлы (НО НЕ те, что сохранили!)
                for item in os.listdir(current_dir):
                    if item not in preserved_files:
                        src = os.path.join(current_dir, item)
                        if os.path.isdir(src):
                            shutil.rmtree(src)
                        else:
                            os.unlink(src)
                
                # Копируем новые файлы из обновления
                for item in os.listdir(extracted_dir):
                    src = os.path.join(extracted_dir, item)
                    dst = os.path.join(current_dir, item)
                    if os.path.isdir(src):
                        shutil.copytree(src, dst)
                    else:
                        shutil.copy2(src, dst)
                
                log.info("✅ Обновлённые файлы скопированы")
            
            # === ВОССТАНОВЛЕНИЕ ПОЛЬЗОВАТЕЛЬСКИХ ДАННЫХ ===
            for filename, src_path in preserved_files.items():
                if filename.endswith("/"):
                    # Это папка
                    dst_dir = os.path.join(current_dir, filename.rstrip("/"))
                    if os.path.exists(dst_dir):
                        shutil.rmtree(dst_dir)
                    shutil.copytree(src_path, dst_dir)
                else:
                    # Это файл
                    dst_file = os.path.join(current_dir, filename)
                    shutil.copy2(src_path, dst_file)
                log.info(f"✅ Восстановлен файл: {filename}")
            
            self._update_status("✅ Все данные сохранены! Перезапуск...")
            
            self._update_status("✅ Обновление завершено! Перезапуск...")
            
            # Ждём 2 секунды и перезапускаемся
            self.after(2000, self._restart_app)
            
        except Exception as e:
            log.error(f"❌ Ошибка обновления: {e}")
            self._update_status(f"❌ Ошибка: {e}")
            self.cancel_btn.config(state=tk.NORMAL)
    
    threading.Thread(target=download_and_update, daemon=True).start()

def _update_progress(self, percent):
    """Обновляет прогресс-бар (вызывается из потока)"""
    self.update_progress.config(text=f"{percent}%")
    self.progress_bar.config(value=percent)

def _update_status(self, text):
    """Обновляет статус (вызывается из потока)"""
    self.update_status.config(text=text)

def cancel_update(self):
    """Отменяет обновление"""
    if self.update_window:
        self.update_window.destroy()
        self.update_window = None

def _restart_app(self):
    """Перезапускает приложение"""
    try:
        self.update_window.destroy()
        self.destroy()
        
        # Перезапуск текущего exe
        import subprocess
        exe_path = sys.executable
        subprocess.Popen([exe_path], cwd=os.path.dirname(exe_path))
        
        sys.exit(0)
    except Exception as e:
        log.error(f"❌ Ошибка перезапуска: {e}")
```

**Изменения в `__init__()`:**
- Убрать `self.show_welcome()` из конца `__init__()`
- Вызывать `self.after(2000, self.check_for_updates)` для проверки обновлений
- После проверки обновлений (или если нет обновлений) вызывать `self.show_welcome()`

---

### Шаг 8: Проверка первого запуска

**В `__init__()`:**
```python
# Проверка первого запуска
self.is_first_run = db.get_user() is None
if self.is_first_run:
    self.after(500, self.show_registration_window)
else:
    # Загружаем имя пользователя
    user = db.get_user()
    self.user_name = user['name'] if user else ""
    self.after(500, self.show_welcome)
```

---

## Порядок выполнения

1. ✅ Создать `user_database.py` (SQLite)
2. ✅ Создать `firebase_sync.py` (Firebase Realtime Database)
3. ✅ Удалить встроенные ключи из `jarvis_fixed.py`
4. ✅ Подключить `user_database.py` и `firebase_sync.py` в `jarvis_fixed.py`
5. ✅ Добавить окно входа/регистрации
6. ✅ Добавить `show_registration_window()`
7. ✅ Добавить `open_profile()` и кнопку "Профиль"
8. ✅ Добавить секцию API ключей в `open_settings()`
9. ✅ Добавить ПОЛНОЦЕННОЕ авто-обновление с прогресс-баром
10. ✅ Удалить mobile из git
11. ✅ Настроить Firebase (создать проект, правила)
12. ✅ Проверить синтаксис
13. ✅ Закоммитить и запушить

---

## Тестирование

1. **Первый запуск:**
   - Должно появиться окно регистрации
   - Ввести имя → сохранить
   - Ввести API ключи → сохранить
   - После закрытия — основной интерфейс

2. **Повторный запуск:**
   - Окно регистрации НЕ появляется
   - Имя пользователя загружается из БД

3. **Кнопка "Профиль":**
   - Показывает текущие данные
   - Можно редактировать

4. **Настройки → API ключи:**
   - Можно добавить/изменить ключи
   - Сохраняются в БД

5. **Удалённые ключи:**
   - В коде нет `GIGACHAT_BUILTIN_KEY`
   - В коде нет `FISH_AUDIO_BUILTIN_KEY`

6. **Авто-обновление:**
   - При запуске проверяется версия на GitHub
   - Если есть новая версия — показывается окно с прогресс-баром
   - Прогресс отображается в процентах (0-100%)
   - Кнопка "Отмена" доступна только до начала загрузки
   - **ПЕРЕД обновлением автоматически сохраняются:**
     - `user_database.db` (данные пользователя: имя, дата рождения)
     - `memory.json` (всё, что запомнил Джарвис)
     - `media_state.json` (состояние медиа, плейлисты)
     - `jarvis_license.json` (подписка)
     - `generated_keys.json` (ключи подписки)
     - `config.json` (настройки)
     - `sounds/` (все пользовательские MP3)
     - `images/` (все пользовательские изображения)
     - `jarvis.log` (логи)
   - После загрузки — распаковка и перезапись файлов
   - **ПОСЛЕ обновления все данные автоматически восстанавливаются**
   - Приложение автоматически перезапускается с новой версией
   - **Все пользовательские данные остаются на месте!**

---

### Инструкция по настройке Firebase (5 минут)

1. **Создаём проект:**
   - Идём на https://console.firebase.google.com/
   - Нажимаем "Добавить проект"
   - Название: `jarvis-ai`
   - Google Analytics: можно выключить
   - Нажимаем "Создать проект"

2. **Включаем Realtime Database:**
   - В меню слева: "Realtime Database"
   - Нажимаем "Создать базу данных"
   - Локация: `us-central1` (или ближайшая)
   - Режим: **Тестовый режим** (потом можно сменить)
   - Нажимаем "Готово"

3. **Скачиваем credentials:**
   - Нажимаем ⚙️ (шестерёнка) → "Project settings"
   - Внизу: "Your apps"
   - Нажимаем `</>` (Web)
   - Название: `JARVIS-DESKTOP`
   - Регистрируем
   - Скачиваем `firebase_credentials.json`
   - Кладём в корень проекта `Jarvis/`

4. **Настраиваем правила безопасности:**
   - В Realtime Database → "Rules"
   - Заменяем на:
   ```json
   {
     "rules": {
       "users": {
         "$uid": {
           ".read": "$uid === auth.uid",
           ".write": "$uid === auth.uid"
         }
       },
       "sync": {
         "$uid": {
           ".read": "$uid === auth.uid",
           ".write": "$uid === auth.uid"
         }
       }
     }
   }
   ```
   - Нажимаем "Опубликовать"

5. **Устанавливаем библиотеку:**
   ```bash
   py -3.13 -m pip install firebase-admin
   ```

6. **Добавляем в `.gitignore`:**
   ```
   firebase_credentials.json
   *.db
   *.sqlite
   mobile/
   ```
| БД не создаётся | Добавить try/except при инициализации |
| Кириллица в путях | Использовать `os.path.abspath()` |
| Конфликт с старыми данными | Миграция БД при изменении схемы |
| Окно регистрации блокирует UI | Использовать `grab_set()` и `Toplevel` |
